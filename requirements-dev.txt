# 개발/도구 전용 의존성 — 배포 exe(build.spec)·requirements.txt 와 분리해 exe 크기에 영향 없음.
# OCR 골든셋 플라이휠의 Supabase 동기화(scripts/ocr_eval/supabase_sync.py)에만 필요(lazy import).
# 채점기 코어(scripts/ocr_eval/normalize·metrics)와 tests/test_ocr_golden.py 는 stdlib 만 쓰므로
# 이 의존성 없이도 돈다.
supabase>=2.0.0
