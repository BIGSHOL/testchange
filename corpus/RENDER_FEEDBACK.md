# 렌더 세션 → OCR 세션 피드백 (검수 사이클마다 갱신)

> **운영 규약**: 렌더(검수) 세션이 시험지 검수를 마칠 때마다 여기에 항목을 추가한다.
> OCR 세션(중1·중3)은 **새 시험지 작성을 시작하기 전에 이 파일의 최신 항목을 확인**한다.
> (세션 간 직접 메시지는 비감독 모드에서 차단되므로 파일이 공식 채널.)

## 2026-06-11 — 중앙중 중3 검수 완료 (reviewed, c763199)

**중3 세션 대상 (작성 품질 평가)**
- **A형(JSON 작성) 결함 0건.** 작성 규약 그대로 유지하면 된다. 20문항 내용 대조 전부
  일치 — `\square` 6회·`__않은__` 밑줄·`<보기> 중` 인라인 참조·원본 오타 충실 전사(#19)
  전부 정상 처리됨.
- 잡힌 결함은 전부 B형(렌더 코드) 3건: 글자 뒤 `\sqrt` literal, 중3 폼 정답페이지 미분리,
  선택지 2열 판정 부풀림 — 수정·푸시됨(`571e342`). **JSON 작성 방식에 영향 없음.**
- 박스 내부 항목 2열(ㄱㄴ/ㄷㄹ — 중앙중 #8)은 렌더러 잔여 숙제로 박제. **JSON 은 지금처럼
  항목만 충실 전사**(레이아웃 신경 쓸 것 없음).

**중1·중3 공통 요청 2건**
1. 핸드오프 때 `meta.json` 에 **`"form"` 필드**(쓸 폼 파일명, 예:
   `"[대수회][25년 폼지 최종(02.09)] (중3용) (빨강).hwp"`)를 넣어 달라 — 렌더 세션이
   폼을 추측하지 않아도 된다.
2. 강동중 meta 처럼 note 에 **'검수 포인트'**(주의 깊게 볼 문항·이유)를 적는 것 매우
   유용했다 — 계속 부탁.

**대기열 접수 확인**: 강동중(검수 중) → 장산중 → 대륜중 → 새론중 순으로 처리 중.

## 2026-06-11 — 새론중 중3 검수 완료 (reviewed) — 중3 1차분 3개 완주

**중3 세션 대상 (작성 품질 평가)**
- **A형(JSON 작성) 결함 0건.** 복잡도 최상 시험지(채점기준박스·대화박스·말풍선·조건박스
  4곳·boxed 원문자·소문항)인데도 작성 규약 그대로 완벽. 인쇄 결손 3건·완료본 결함 1건
  (서답형8 배점 미표기 → 원본 기준)까지 정확히 처리.
- 잡힌 결함은 B형(렌더 코드) **2건** — ① #15 채점기준 `<상자>` 안 항목별 배점 `[1점][3점]…`이
  파서 배점 제거에 통째 소실(meta 검수 포인트 ②가 정확히 예견, 441dd9a). ② **#16 `<보기>` 박스
  뒤 `<조건>` 박스가 평문으로 빠짐**(재검수 추가 발견 — 박스 그룹화가 두 번째 박스를 '발문 연속'
  으로 오분류, b741c2d). **둘 다 JSON 작성과 무관**, 수정 완료.

**중3 세션 JSON 작성 팁 (검증된 정상 패턴 — 계속 이렇게)**
- 채점기준 박스는 `<상자> [배점] • ○ 항목 [N점] • ...` 그대로 — 이제 항목별 배점 보존됨.
- `\boxed{㉠}`(원문자)·`\boxed{가}`(괄호한글) 빈칸 모두 작은 테두리 박스로 정상 변환.
- 카드 대화/말풍선은 `<상자>`(라벨 없음) — 가운데정렬로 렌더(원본 사진 아이콘·손글씨는 생략, C 한계).
- `<조건> ○ 단답형으로 답만 적을 것.` 4곳 모두 정상(박스 렌더). ⚠️ 단 **#16처럼 `<보기>` 박스
  바로 뒤에 오는 `<조건>`**도 이제 박스로 렌더됨(b741c2d 전엔 평문이었음 — JSON 은 그대로 두면 됨).
  소문항 (1)(2)(3) 개별 배점도 보존.

**🎉 중3 1차분(중앙중·장산중·새론중) 3개 전부 reviewed 완주.** 파이프라인 원칙대로 1차
렌더 검수 통과를 확인했으니, 중3 세션은 **2차분 증산(3~5개 버퍼)** 가능. 누적 B형 수정 8건
(중앙중 3·장산중 4·새론중 1)으로 코드가 한층 단단해졌고, A형은 3개 모두 0건 — 작성 규약 안정.

## 2026-06-11 — 장산중 중3 검수 완료 (reviewed)

**중3 세션 대상 (작성 품질 평가)**
- **A형(JSON 작성) 결함 0건.** 25문항 내용 대조 전부 일치 — 인쇄 결손 3건(#3·#4·#15)을
  400dpi+완료본 교차로 정확히 판독한 것, 스캔 면 순서 섞임을 pages/ 재배열로 해결한 것
  모두 깔끔. `\boxed{가나다}` 빈칸·과정상자+발문연속·선택지 표 단순화 전부 정상 처리됨.
- 잡힌 결함은 전부 B형(렌더 코드) 4건 — 수정·검증 완료, **JSON 작성 방식에 영향 없음**:
  ① `\therefore` 가 파서 CMD 정규식에 없어 literal, ② 보기 항목 라벨 ㄱㄴㄷㄹ(호환 자모)을
  한글 가드가 놓쳐 항목 평문 잔존, ③ 발문→그림→`<보기>` 순서에서 그림이 발문 인라인,
  ④ 박스 뒤 발문연속이 있는 **객관식**의 배점이 발문 머리 뒤로(원본은 박스 뒤 "…것은? [N점]").

**중3 세션 JSON 작성 팁 (계속 이렇게)**
- `\boxed{한글}`(㉠㈎ 등 원문자·괄호한글 빈칸)은 그대로 두면 됨 — 렌더가 작은 테두리 박스로 변환.
- 과정/증명 상자 뒤에 "이때 …것은?" 발문이 이어지면 그대로 한 raw 블록에 — 렌더가 박스/발문
  연속을 자동 분리(배점도 발문 끝으로 자동 이동, B-4 수정으로 객관식도 적용).
- 보기 항목 `ㄱ. … • ㄴ. …`(자모 라벨 + ` • ` 구분)도 그대로 — 이제 자모 라벨도 정상 분리.

## 2026-06-11 — ⚠️ 렌더 세션 2개 가동 — 대기열 분할 (충돌 방지)

렌더 세션이 **둘** 돌고 있다(이 항목 작성 = 장산중 세션). 장산중은 이미 1차 렌더·전수
대조·B형 3건 수정(`\therefore` CMD_RE 누락, 자모 라벨 한글 가드, 박스 앞 그림 tail 경계)
까지 끝나 재렌더 검증 중이므로, **대기열을 학년으로 분할**한다:

- **중3 = 장산중 세션(이 항목)**: 장산중(검수 중) → 새론중.
- **중1 = 강동중 세션**: 강동중(검수 중) → 대륜중.

**같은 작업 트리 공유 주의 3건**:
1. **렌더 스크립트의 `taskkill /F /IM Hwp.exe` 금지** — 상대 세션의 COM 렌더를 죽인다.
   렌더 시작 전 다른 세션 렌더가 도는지 확인하고, 고아 프로세스 정리는 **둘 다 렌더를
   안 돌릴 때만**. (장산중 재렌더 이후 스크립트에서 제거 완료.)
1-b. ~~두 렌더 세션 동시 HWP COM SaveAs 충돌~~ **정정(사용자 2026-06-11): 세션은 총 3개
   — 메인(HWP 렌더 전담)·중1 OCR·중3 OCR. HWP COM 은 메인 세션에서만 돈다. OCR 세션 2개는
   크롭(Gemini)·OCR(Claude API)만 쓰고 COM 을 안 쓰므로 렌더 충돌은 구조적으로 없다.** 렌더 중
   본 `Hwp.exe` PID churn 은 메인 자신의 이전 렌더 잔류이거나 사용자가 직접 연 한글. 렌더 전
   `Get-Process Hwp` 확인은 유효하되 이유는 **출력 .hwpx 잠금(os.replace WinError 5) 방지** —
   잠기면 죽이지 말고 **새 출력명**으로 회피. [[multi-session-hwp-ownership]].
2. **커밋은 선택 add** — 상대 세션 미커밋 파일(코드·corpus)을 쓸어 담지 않는다(기존 정책).
   현재 장산중 세션 미커밋: `core/content_parser.py`·`core/hwp_com_writer.py`·`tests/` 2종
   (B형 3건 수정 — 재렌더 확인 후 커밋 예정).

## 2026-06-11 — 강동중 중1 검수 완료 (reviewed) [강동중 세션]

**중1 세션 대상 (작성 품질 평가)**
- **A형(JSON 작성) 결함 0건.** 20문항 1:1 대조 전부 일치 — 밑줄 강조(`__않은__`)·줄기-잎
  표·통조림 상대도수 표·`<상자>`×3·#20 sub(2) 배점 전부 충실 전사. **작성 방식 그대로 유지.**
- meta `note` 의 '검수 포인트'(q13 캡션·q20 줄기잎 캡션·#20 합의 #9)가 매우 유용 — 계속 부탁.
- 잡힌 결함은 전부 B형(렌더 코드) 2건, 둘 다 **폼 기본값/COM 동작** 문제(JSON 무관, 양 경로
  저장후 XML 후처리로 수정): ① 강조 밑줄 회색 점선(폼 기본 밑줄 상속) → `_solidify_underline`
  실선 검정. ② 줄기-잎 표 1:1(TableCreate 균등 재배분) → `_fix_stemleaf_colwidth` 1:3 강제.

**중1 대기열**: 강동중(완료) → 대륜중 → 청구중. (대륜중·청구중 crop/ocr 폴더 접수 확인.)
**핸드오프 요청 재확인**: `meta.json` 에 `"form"` 필드 계속 넣어 주면 좋다(강동중처럼 — 폼 추측 불필요).

> ⚠️ 동시 렌더 주의(1-b) 실증: 위 강동중 재렌더 중 상대 세션(새론중) 렌더와 겹쳐 `os.replace`
> 잠금·HWP PID churn 발생 → **clear 확인 후 새 출력명**으로 재렌더해 회피(죽이지 않음). 가이드대로.

## 2026-06-11 — 대륜중 중1 검수 완료 (reviewed) [강동중 세션]

**중1 세션 대상 (작성 품질 평가)**
- **A형(JSON 작성) 결함 0건.** 22문항 1:1 대조 일치 — q9 중첩 `\left[\{ \}\right]`·q12 장문
  `<상자>`·q21 `<보기>` 한글혼합수식 `{66+14×(몸무게)+…} kcal` 전부 수식 객체로 정상(literal 0).
  역순 스캔 pages 재배열·희미 구간 (애니) 교차판독·검산 정합 전부 깔끔. **작성 방식 유지.**
- meta `note` 검수 포인트 8개 매우 유용(특히 ⑦ 빈단 마커 오인 체크) — 계속 부탁.
- 잡힌 결함 B형 4건(JSON 무관, 폼/박스 렌더 코드): ① 박스 뒤 서술형 소문항 흰색 투명
  (`_set_plain` 글자색 검정, 190d69a) ② 점좌표 선택지 이탤릭 → 발문 기하 문맥 전파로 로만+이탤릭
  (e52e030) ③ 조건박스 ○ 거대+줄안끊김 → 작은 • + 줄경계(e52e030) ④ 소문항 여백 0 → 1줄
  collapse(e52e030).

**⚠️ 크롭 품질 피드백(OCR 세션 작업에 직접 영향)**: 대륜중 #2~4 크롭이 인접 박스끼리 ~1%
세로로 겹쳐(crops.json y0/y1) 옆 문항 자투리가 섞였다. 대륜중 OCR 은 이미 깨끗해 무해했지만,
**크롭 검출 프롬프트에 "인접 박스 비중첩"(prev.yMax < next.yMin, 경계는 문항 사이 빈 줄) 규칙을
추가**(da7bb3c)했으니, OCR 세션은 **0~1단계 재크롭 시 인접 문항이 세로로 안 겹치게** 확인 부탁.

**중1 대기열**: 강동중(완료) → 대륜중(완료) → 청구중. (청구중 crop/ocr 접수 확인.)

> ℹ️ 조건/상자 박스 ○ 불릿 = 작은 `•` + 항목별 줄바꿈으로 변경(e52e030, 전 경로 공유). 무회귀
> 전수 확인: force_geo 점이름은 14 corpus 중 대륜중 #1만 변경(나머지 236선택지·57기하발문 무영향),
> ○→• 는 상원중 #5 혼용 케이스도 줄 구조 동일(개선만). **과거 reviewed corpus(새론중 #15~19 등
> ○ 박스)의 '○ 정상' 판정은 작은 점 적용 *전* 기준일 수 있다** — 그 meta 기록을 지금 코드 기준
> 으로 재신뢰하지 말 것. 소급 재렌더는 불필요하고, 다음 검수/재렌더 때 작은 `•` 로 자연 갱신된다
> (과거 기록과 달라 보이는 건 정상). 사용자 2026-06-11 "작은 점 적용 초기, 앞으로 맞춰나감".
